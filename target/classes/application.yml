spring:
  datasource:
    url: jdbc:postgresql://yamanote.proxy.rlwy.net:20745/railway?user=postgres&password=DwfgMxadDktOryeliUPksZnYszzgckGB&sslmode=disable
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    database-platform: org.hibernate.dialect.PostgreSQLDialect

  flyway:
    enabled: false

server:
  port: 8080
