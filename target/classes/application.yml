spring:
  datasource:
    url: jdbc:postgresql://postgres.railway.internal:5432/railway?user=postgres&password=DwfgMxadDktOryeliUPksZnYszzgckGB
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    database-platform: org.hibernate.dialect.PostgreSQLDialect

  flyway:
    enabled: false

server:
  port: 8080
